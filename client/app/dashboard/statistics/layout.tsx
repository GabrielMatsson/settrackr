import StatisticsShell from "./components/StatisticsShell"

export default function StatisticsLayout({ children }: { children: React.ReactNode }) {
  return <StatisticsShell>{children}</StatisticsShell>
}
